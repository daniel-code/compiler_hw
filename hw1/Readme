1. Lex, Yacc 版本
	flex 2.5.35
2. 作業平台
	ubuntu 14.04 LTS
3. 執行方式
	(1)解壓縮後，直接輸入 ./test_all 即可以知道全部的測試結果
	(2)輸入make 產生 a.out 再利用導入的方式，導入想要測試的檔案
4. 你/妳如何處理這份規格書上的問題
	(1)保留字：直接將所有的保留字列出來，然後將每一個保留字再用大小寫寫成regular expressions
	(2)識別字：因為第一個字有特別限定只能為A-Za-z或是底線(_)符號，因此將這個特性另外寫成判斷式，或是只有單一個字也能構成為ID
	(3)整數：整數主要由0-9組成，另外開頭加上正負符號的判斷，因為不能夠出現00或是012這種整數，所以在另外處理數字開頭的部分
	(4)實數：實數主要是利用整數組成，但是其中小數的部分剛好跟整數相反，不能出現0.500這種小數，所以在小數的部份另外做判斷
	(5)字串常數：
	(6)註解
	(7)
5. 你/妳寫這個作業所遇到的問題
